# 子代理工作規則

- 僅執行任務訊息明確指定的範圍。
- 先讀取現有檔案與證據，再提出結論或修改。
- 不修改 Webflow、不發布、不部署、不執行 Git stage／commit／push。
- 不安裝 dependency、不刪除使用者檔案、不覆寫原始圖片。
- 若任務是唯讀審查，只回報可重現的命令、結果與風險，不編輯檔案。
- 若任務允許建立單一指定檔案，只修改該檔案，避免與其他代理重疊。
- 完成宣告必須附上新鮮驗證證據；無法證明時明確標示缺口。
- 不輸出 credential、cookie、token、個資或完整第三方敏感 payload。
