# 紅河隱園網站開發指令

## 專案範圍

- 本專案是簡單的繁體中文形象網站，預設只有前端、靜態內容與聯絡表單。
- 不自行新增後端、資料庫、CMS、登入、付款、監控或分析服務；需要時先確認。
- 本機檔案是後續開發與部署的 source of truth。Webflow 只作為遷移參考，未經明確授權不得回寫或發布。
- 當前對話中的最新明確指示優先於本檔。

## 工作方式

- 修改前先讀本檔、相關檔案，以及 Git branch／dirty tree；尚未初始化 Git 時明確回報。
- 保留既有變更。小改動直接在目前工作區完成；大範圍重構先說明影響。
- 缺少次要資訊時採保守假設並前進；若會改變文案、網址、功能、費用或外部狀態，先詢問。
- 不建立重複的計畫、追蹤表或文件。

## 子代理

- 只把明確、獨立、可驗證的工作交給子代理。
- 子代理只能使用 GPT-5.6 Luna、思考模式 `max`，並先讀根目錄 `AGENTS.sub.md`。
- 子代理回報只算代理證據；主代理必須獨立驗證後才能宣告完成。
- MiniMax Code 是共享 workspace 的協作執行者，不是 Codex 子代理；角色與交付規則見 `docs/collaboration/agent-roles.md`。

## 文案與圖片

- 使用台灣繁體中文。使用者提供或手動修改的文案視為 frozen copy，不擅自改寫。
- `imports/webflow/assets/images/` 保存原圖，不覆寫、不刪除。
- 輕量化圖片放在獨立目錄，保留原圖對照、尺寸、格式與來源 manifest。
- 最終網站應自管資產，不新增 Webflow CDN 相依。
- 內容圖片提供有意義的 `alt`；純裝飾圖片使用空 `alt`。

## Dependency、秘密與刪除

- 新增 dependency 前先說明用途、替代方案、開發／正式環境影響，取得同意後再修改。
- 不提交 API key、token、cookie、`.env*` 或其他秘密。
- 不在 log、文件或對話輸出 credential、個資或完整第三方 payload。
- 批量刪除、遠端刪除與覆寫原始資產必須先取得明確授權。

## 驗證

- 只跑與本輪修改相關的檢查；完成宣告必須附上新鮮證據。
- 網頁修改至少檢查內部連結、資產路徑、主要響應式尺寸與 404。
- 圖片修改至少檢查可解碼、尺寸、透明度、檔案大小與原圖保留狀態。
- 本機結果不能冒充正式站或部署成功證據。

## Git 與部署

- 未經明確授權，不執行 stage、commit、push、PR、DNS 變更、Webflow publish 或任何部署。
- Production 部署每次都需單獨確認。
- Commit 未另訂規範時使用 `[type] 簡短描述`。

## 清理

- 每輪結束時清理自己產生的暫存檔、失敗輸出與一次性腳本。
- 保留原始資產、manifest、可重跑腳本及後續遷移仍會使用的檔案。
